# Nastavna baza Goč - Project Context

## Project Overview
"Nastavna baza Goč" (Teaching Base Goč) is a web platform for the University of Belgrade Faculty of Forestry. It provides dynamic content management, accommodation bookings, guest CRM, and a portal for visitors.

## Tech Stack & Design Identity
-   **Frontend:** Vue 3 (Composition API, `<script setup>`), Vue Router, Pinia
-   **Backend:** Node.js, Express, MySQL (Aiven cloud), Nodemailer (Brevo SMTP via API keys)
-   **Styling (Forest Vibe):** Custom CSS (Modular, embedded in Vue SFCs). 
    - Strict square flat design (no rounded corners `border-radius: 0`).
    - Color palette migrated from brown to **"Deep Forest Green"**: `#0D3018` (Footer wrapper, Hero overlay with 0.65 opacity), `--c-green-1` to `--c-green-6`.
    - Hero slides and cards use full-bleed Unsplash imagery with transparent white text boxes (`rgba(255, 255, 255, 0.66)`) at the bottom.

## Architecture & Integration Details
-   **Public Portal:** Home (`/`), Accommodation (`/smestaj`), News (`/vesti`), Contact/About (pointing to Under Construction templates via `NotFoundView.vue`).
-   **Guest Portal:** `/prijava` and `/moj-nalog`. Guests can view reservations, cancel (up to 7 days before), and redeem Loyalty Vouchers on-the-fly.
-   **CMS / Admin:** `/admin/login`, `/admin/vesti`, `/admin/rezervacije`, `/admin/gosti`. Secured via JWT (`admin_token`).
-   **AI Chat Agent (Status Gap):** 
    - The Node backend HAS a `chat_messages` table and routes (`/api/chat/history`) to persist history for both logged-in (`guest_id`) and anonymous (`session_id`) users.
    - **Current Gap:** The Vue frontend (`chat.js` Pinia store) only holds history in RAM (cleared on refresh). Furthermore, `agentService.js` currently bypasses the local backend entirely and points to an external agent endpoint (`https://chat-agent-kbjc.onrender.com/api/chat`). **Next Action Required:** Route frontend chat calls through the local Node backend, utilize `/api/chat/history` on load, and pipe the Anthropic API call directly from the main backend to unify history and eliminate the separate Render service.

## Database Schema (Key Tables)
1.  **pages**: Dynamic page translations (`page_slug`, `title`, `text_content`, `lang`).
2.  **hero_slides**: Carousel content linked per page (`page_slug`, `image_url`, `target_link`).
3.  **news**: Dynamic news listings (`id`, `title`, `content_text`, `image_url`).
4.  **inquiries**: Holds booking requests. Statuses check against availability.
5.  **guests**: `(id, email, password_hash, name, phone, reservation_count, vouchers JSON, created_at)`. Tracks loyalty and vouchers.

## Phase 3 Completion Status
We successfully implemented the **CRM, Vouchers and Guest Portal** system.
-   [x] The `guests` table now tracks `reservation_count` and holds a `vouchers` JSON column.
-   [x] Admin panel (`/admin/gosti`) lists guests and allows distributing custom digital vouchers via a pop-up.
-   [x] Guest dashboard (`/moj-nalog`) displays My Reservations, Change Password and **My Vouchers**.
-   [x] Vouchers are redeemed directly by the Guest on their phone in front of Staff ("Samo-poništavanje"). No Staff tablets needed.
-   [x] Upsell / Recommendation box implemented for loyal users.

## Recent Features & Fixes
-   **Forest Vibe UI Remaster:** Transformed the global style to a lush green aesthetic with updated hero transparencies, new footer colors, improved button hovers (restored icon `✉` on inquiry), and strict `object-position: top center` for images.
-   **Database Imagery Cleanup:** Purged old broken links and set uniform, high-quality Unsplash photography for all rooms (`photo-1587061949409-02df41d5e562`) and facilities. Set unified `/placeholder-staff.svg` vector icons for all staff members.
-   **News UI (Full Cover):** Redesigned the `PageTemplate.vue` news grid/carousel to use absolute full-cover images with an overlaid white semi-transparent text box.

## Next Steps / Future Ideas
-   **Refactor AI Chat Workflow:** Wire `agentService.js` to the local backend to properly hydrate and persist `chat_messages` from the MySQL DB on page load.
-   **Expand `.vesti` content:** Render detailed single-page views for News if needed.
